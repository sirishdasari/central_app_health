name: central_app
description: "MyDaily Health add-in"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.6.0

dependencies:
  flutter:
    sdk: flutter
  health: ^13.1.3
  appwrite: ^24.2.0
  permission_handler: ^12.0.3
  workmanager: ^0.10.10
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
